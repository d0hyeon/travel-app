# Travel App

여행 계획 및 정산 관리 앱.

## 개발 명령어

```bash
yarn dev          # 개발 서버
yarn build        # 프로덕션 빌드
yarn ts-check     # 타입 검사
yarn lint         # ESLint
yarn gen-types    # Supabase DB 타입 재생성
```
