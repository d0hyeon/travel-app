# 작업 규칙

## 문서 맵

| 문서                | 경로               | 언제 읽는가                                            | 읽은 후 출력              |
| ------------------- | ------------------ | ------------------------------------------------------ | ------------------------- |
| 코드베이스 레퍼런스 | `docs/codebase.md` | 파일 위치 모르거나, 처음 파악할 때, 기능 탐색 시작 시  | `[init:read:codebase.md]` |
| 테스트 가이드       | `docs/testing.md`  | 테스트 작성 전, 단위/통합/E2E 중 무엇을 쓸지 결정할 때 | `[init:read:testing.md]`  |

## 테스트 작성

- `docs/testing.md` → 테스트 계층 구조, 단위 vs 통합 판단 기준, 도구 설정
- 필요 시 `docs/codebase.md` → 테스트 파일 위치 규칙 (`__tests__/` 위치) 확인

## 작업 파이프라인

### 모든 작업 시작 시

**반드시** `docs/codebase.md`를 읽는다. 예외 없음.

### 전략 선택

**사용자에게 묻지 않는다.** 아래 중 **하나만** 고른다. 둘을 겹쳐 쓰지 않는다.
(둘 다 "구현 전 설계 확정"이라는 같은 역할을 하므로 겹치면 중복이 된다.)

판단 기준은 **조율할 것이 있는가**다.

| 상황                             | 선택 |
| -------------------------------- | ---- |
| 단일 모듈, 순수 로직 중심        | A    |
| 여러 파일·태스크, 순서 조율 필요 | B    |

#### A. 설계 우선 TDD — 단일 모듈, 순수 로직 중심

산출물이 테스트 파일로 코드베이스에 영구히 남는다.

1. **인터페이스 확정** — 타입·시그니처를 먼저 쓴다.
2. **검증 케이스 나열** — `it.todo('...')` 로 제목만 적는다.
   목적은 테스트가 아니라 **설계 검증**이다.
   케이스를 쓰다가 인터페이스 결함이 보이면 1로 돌아간다.
3. **하나씩 채운다** — `it.todo` 를 실제 테스트로 바꾸고,
   올바른 이유로 실패하는지 확인한 뒤 최소 구현으로 통과시킨다.

#### B. writing-plans — 여러 파일·태스크, 순서 조율 필요

산출물은 플랜 문서이며 실행이 끝나면 수명이 다한다.

플랜 작성 시:

- 플랜에는 **인터페이스와 검증 케이스 제목까지만** 담는다.
  테스트 본문은 담지 않는다 — 실행 시점에 A의 3단계로 채운다.

플랜 실행 시:

- 기본값은 `superpowers:subagent-driven-development` 다.
- 태스크 간 의존 그래프를 그려 웨이브로 묶고, 의존이 만족된 태스크는 병렬 실행한다.
  - 같은 파일을 수정하는 태스크는 같은 웨이브에 넣지 않는다.
  - 웨이브 시작 전 해당 태스크 목록을 한 줄로 보고한다.
- 존재하지 않는 모듈을 가정하고 구현하지 않는다.
  각 태스크는 의존 태스크가 완료된 뒤 시작해 빌드·테스트 검증이 항상 유효해야 한다.
- 플랜에 명시된 검증 케이스를 임의로 바꾸지 않는다.
  구현이 테스트를 통과하지 못하면 테스트를 고치지 말고 보고한 뒤 멈춘다.

#### 적용하지 않는 대상

- UI 컴포넌트 (`*.tsx`) — 컴포넌트 테스트 인프라가 없다.
  빌드와 실제 앱 확인으로 검증한다.
- React Query 훅 — 순수 로직을 `*.utils.ts` 로 분리해 그쪽을 테스트한다.

### 작업 완료 후

`docs/codebase.md`에 변경사항을 업데이트 한다.
업데이트 전에 파일 위치·의존 방향이 문서와 어긋나지 않는지 함께 확인한다.

## 작업 단위 규칙

- 작업의 볼륨이 크고 작업을 여러 단위로 분류 가능하다면, 워크트리 및 브랜치로 하위 작업을 분리한다.
- 작업이 완료되는 경우 루트 브랜치로 PR 혹은 병합한다.
- 브랜치 당 변경되는 코드의 수를 500개 이하로 목표한다.

```
- feat/weather (Root)
  - feat/weather-api (Work)
  - feat/weather-detail-screen (Work)
  - feat/weather-detail-trigger (Work)
```

# 커밋 규칙

## 커밋 단위

커밋은 독립적으로 설명·검증·되돌릴 수 있는 최소 변경 목적 단위로 나눈다.

### 판단 기준

다음 질문 중 하나라도 "예"라면 반드시 커밋을 분리한다.

1. 변경 이유를 서로 다른 문장으로 설명할 수 있는가?
2. 일부 변경만 독립적으로 테스트하거나 리뷰할 수 있는가?
3. 일부 변경만 되돌려도 나머지 변경이 여전히 의미가 있는가?
4. `feat`, `fix`, `refactor`, `test`, `docs`, `chore` 등 변경 성격이 다른가?
5. API, 도메인 모델, UI, 화면 연결, 공용 인프라처럼 책임 영역이 다른가?

여러 파일을 하나의 커밋으로 묶는 것은 다음 조건을 모두 만족할 때만 허용한다.

- 모든 파일이 정확히 하나의 변경 목적을 구현한다.
- 어느 파일도 별도의 목적이나 정리를 포함하지 않는다.
- 커밋 메시지 한 줄로 모든 변경 이유를 구체적으로 설명할 수 있다.
- 커밋 일부만 따로 되돌리는 것이 의미가 없다.

"하나의 기능을 완성하기 위해 필요하다"는 이유만으로 단일 커밋으로 묶지 않는다.
하나의 기능도 API, 도메인 로직, UI, 화면 연결, 버그 수정 등 독립된 목적이 있으면 나누어 커밋한다.

테스트는 해당 동작을 구현한 커밋에 포함한다.
문서는 특정 변경을 설명하는 경우 해당 커밋에 포함한다.

## 커밋 메세지

1. 커밋 메세지는 한글로 작성하며, 커밋 단위 규칙을 준수하여 짧은 한문장으로 충분히 표현 할 수 있도록 한다.
2. 커밋 메세지에 변경 지점을 직접 남기는것 보다 스코프를 활용한다.

## 공통사항

- 요구사항을 받으면 바로 실행하지 말고 먼저 고민하기
- 더 나은 대안이 있다면 적극적으로 반박하고 제안하기
- 피드백을 적극 해주고 잘못된 판단을 즉시 지적하기
- 문서 내용과 실제 코드가 다르다면 문서를 최신화하기
