# @expo/cli가 metro-runtime을 pnpm 격리 너머에서 찾지 못해 번들링이 실패한다.
# 광범위한 *metro* / *react-native* 패턴은 react-native 자체를 루트로 끌어올려
# 웹 테스트의 React 인스턴스를 오염시키므로, 필요한 것만 정확히 지정한다.
public-hoist-pattern[]=metro-runtime
